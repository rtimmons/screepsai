var assert = require('assert');

// Some better way than modifying global?
global.roleBuilder   = require('../../src/role.builder.js');
global.roleHarvester = require('../../src/role.harvester.js');

describe('harvester', () => {
  it('moves to source if has capacity', () => {
    // TODO
  });
});
